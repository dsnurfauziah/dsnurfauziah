with
laptop as (
  select
  count(device_type) as laptop_view
  from viewership
  where device_type = 'laptop'
),
mobile as (
  select
  count(device_type) as mobile_view
  from viewership
  where device_type in ('phone', 'tablet')
)

select(laptop_view, mobile_view)
from laptop, mobile

